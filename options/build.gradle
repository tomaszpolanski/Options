
allprojects {
    repositories {
        jcenter()
        maven { url System.getenv().get("ANDROID_HOME") + "/extras/android/m2repository" }
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
